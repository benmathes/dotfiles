#!/usr/bin/env bash
# Auto-approve Claude Code's built-in bash syntax safety checks.
# These fire on newlines, $() substitution, $'...' ANSI quoting, etc.
# Exit 0 = approve. Exit 2 = block. No output = silent pass-through.


#!/usr/bin/env bash
cat > /tmp/claude-hook-debug.json
exit 0

